# Roadmap Changelog

A running, per-slice log of **every feature / UI / UX change**. Claude appends an entry as
each change is made; the **port ritual** (on demand or at close session) lists the ⬜ pending
items and asks Peter which to port into the Vision (`index.html`).

**Status key:** ⬜ pending decision · 🌐 ported to the Vision · 🔀 slice-only (won't port)

> How it works: edit/iterate on a slice page → each change logged here as ⬜ → at close
> session (or when Peter asks "what should we port?") Claude lists the ⬜ items → Peter
> picks → Claude ports the chosen ones into `index.html` (build → verify render → push)
> and marks them 🌐, the rest 🔀. See `README.md` for the full workflow.

> **2026-08-01 — Vision + Slices migration:** slices moved from git branches to pages on
> `main` (`slices/<name>.html`). Entries below predating the migration reference the old
> `prd/...` branches; their content now lives in the corresponding slice page.

---

## `index.html` (the Vision — production)
Changes here are the funnel destination; they're not logged as pending. See
`main-production-vision.md` for the current full feature inventory and `session-log.md` for
history.

---

## `slices/video-on-demand.html` (Video On Demand + User Accounts + Feedback Form — one feature, one release)

Copied from the Vision 2026-08-01 (it already contains the full video-library /
on-demand experience). **2026-08-04: VOD and User Accounts merged into one feature that
ships together** (see `decisions.md`); this slice is the single prototype home for both.
The planned separate `user-accounts` slice is dead. Spec: `prd-vod-user-accounts.md`
(its §8 release scope settles this slice's scope-trim debt).

| Date | Change | Status |
|---|---|---|
| 2026-08-01 | Slice created as a copy of the Vision (`index.html`). | meta (creation, not a feature port) |
| 2026-08-03 | Base re-pinned @ 5d6b62c — cosmetic reintegration (base gained meta-only front matter/title changes; no design change). | meta |
| 2026-08-03 | Base re-pinned @ ea6a41a — statuses-removal meta edit; cosmetic. | meta |
| 2026-08-04 | Scope merged with User Accounts (ship together; no separate accounts slice). PRD written: `Roadmap/prd-vod-user-accounts.md`. No file/design change yet — gating UX to be built here per the PRD. | meta (scope + spec, not a feature port) |
| 2026-08-05 | Demo sidebar: **BETA pill** (orange, under EVENT, Home tab) toggles `BETA_BUILD` — the beta-feedback card in the More menu shows only when on (default on). Prototype chrome, not app design. | 🌐 ported (applied to the Vision in the same commit — chrome skipped the funnel wait per Peter) |
| 2026-08-05 | **User accounts — the whole PRD build.** `signedIn` state + demo user (Mary Thompson). **In-place gates** (blur + gold lock + "It's optional" + Join Free pill) on Watch **Continue Watching** and Listen **Continue Listening**; reminder toggles (all entry points, incl. hero pill + end screen) gate via **moment-of-action Join sheet**, with the tapped reminder auto-set after joining. | 🌐 |
| 2026-08-05 | **Join Free sheet** — Apple / Google / email+password, contextual headline (reminder intent vs. generic), "Not now", "Free forever · No spam" footnote. Any method signs in instantly (prototype). | 🌐 |
| 2026-08-05 | **Notification prime-then-prompt** (PRD §7) — after the first reminder is set: "Never miss a prayer" priming card (with the prayer's time) → mock iOS permission dialog. Fires once per session. | 🌐 |
| 2026-08-05 | **Account menu reorg** — identity header first (signed out: blue Join Free card; signed in: MT avatar + name/email + Sign out), then Prayer Requests · Give Now · beta card (moved below Give Now) · **Daily Prayer Reminders page inside the menu** (blue header + the exact Watch-tab rows, "N on" count on the row) · rest of menu + toggles untouched. Header person icon shows **initials** when signed in. | 🌐 |
| 2026-08-05 | Signed-out display state: reminders/resume data hidden while signed out (gates return); anonymous reminders default all-off (was Mass-on). | 🌐 |
| 2026-08-05 | **Resume memory gated everywhere it appears** (Peter's catch): the per-series **Continue Watching block on Series Detail** gets the same in-place gate (lock + blur + Join Free); episode-list progress bars / % / ✓ Watched markers hidden while signed out; hero carousel CONTINUE slides hidden while signed out. All restore on join. | 🌐 |
| 2026-08-14 | Skeleton follow-ups (Peter): **Continue Listening rows** gained the 48px square artwork block on the left, so a loading row reads as a row-with-cover-art rather than three stacked paragraphs (Home and Listen both). **Hero skeleton now matches the featured card exactly at 343×270** — its bar heights and gap sum to the real body's 76px (14 top pad + 20 + 12 + 14 + 16 bottom), where it had been 8px short. **All Series grid** moved onto the same card treatment: 2-up cards with a 16:9 grey artwork area over a white body carrying title and meta bars, instead of the older loose-bars-under-a-rectangle shape. | 🌐 |
| 2026-08-14 | **Skeletons rebuilt to the real card anatomy, and the pulse replaced with a left-to-right shimmer** (Peter). Previous version drew a plain rounded rect with two loose bars floating beneath it — that reads as an article, not as a loading video card. A skeleton card is now the REAL card greyed out: same width, radius, border, shadow and body padding, with a darker 16:9 artwork area over a **white** body carrying the title and meta bars. Measured against the live cards: 155×135 vs 155×136, 220×177 vs 220×179, and the hero mirrors the featured card exactly. **Animation:** the old breath faded to `#f5f5f5`, the same tone as the page background, so skeletons disappeared at the bottom of every cycle. Now a fixed `#dfe3e7` base with a white highlight sweeping left→right (1.5s) — always visible, never vanishing. One shared keyframe so the page sweeps in step. | 🌐 |
| 2026-08-14 | **LOADING / ERROR demo states** (`add loading + error states` command). Two pills in the right gutter, anchored to the bottom of the phone frame as their own group, driving one shared `demoState` that **persists across tab and page navigation** so you can walk Home → Watch → Listen in one pass. Built as a pattern, not a one-off: `<Skeleton>`, `<LoadFailed>` and a `<DataRegion label skeleton>` wrapper mean a future row opts in with one line, plus `SkelRow` / `SkelList` shapes. Skeletons share ONE keyframe animation (1.5s ease-in-out alternate, #ececec ⇄ #f5f5f5) so the page breathes together instead of drifting. "Try again" plays the full arc — error → skeleton → loaded (~1.4s) — verified. **Twenty regions wired:** Articles carousel; Home + Listen Continue Listening; Watch hero, Daily Prayer Reminders, Continue Watching, New Episodes, Featured Series, Fr. Rocky Teaching, Live Prayer, Shows, Conferences & Events, Formation, Documentaries & Films, search results; series page hero/info, its Continue Watching, and episode list; All Series grid; Listening History. **Chrome always stays real** — blue headers, bottom nav, Now Playing bar, section headings, category chips, "All Series →", and the series page's floating back/share discs. **Signed-out gates take precedence over demo states** on every resume surface: not having an account isn't a failed fetch. Errors are per-region and never full-screen. | 🌐 |
| 2026-08-14 | Regions deliberately left alone in that pass, per Peter: promo banner (XML-fed, just doesn't appear), Featured row (bundled default images), sponsor banner (ad-served), Now Playing bar (built into the app, has a placeholder), and all four audiobook surfaces — Continue Listening 2×2 grid, Spiritual Reading, Classic Fiction, and the audiobook detail page. | 🌐 |
| 2026-08-13 | **Jack Cote's show/series artwork wired in** — five new title cards: Life Is Worth Living, Divine Mercy Chaplet, Daily Mass, SEEK 2026 ("To The Heights"), and Trending with Timmerie. **Timmerie had no artwork at all before** (gradient only), so it gained a `SERIES_IMAGES` entry and a card `image` prop; the other four replaced existing files via `SERIES_IMAGES` + hardcoded card/hero refs. Sources were 4K–8K PNGs totalling **17MB**; downscaled to 1600×900 JPEGs at q82, **17MB → 1.1MB**, which matters for the iPad-over-LAN demo. Originals left untracked in `rr_shows/new/`. `seek-2025` deliberately stays on the old still — the new art is branded SEEK 2026. **Known consequence:** these are type-heavy title cards and one image serves both the big surfaces and the 110×64 episode-row thumbnails, so episode rows show the same card repeated and the baked-in type is unreadable at that size. Fix is per-episode stills, which don't exist yet. | 🌐 |
| 2026-08-13 | Gate copy unified across **all five** frosted windows: headline "Save your place with an account." everywhere; sub "Login or create your free account." on all but the series page, which stays headline-only. | 🌐 |
| 2026-08-13 | Series-page description switched from gray `wC.text3` to black `wC.text1` (Peter) — it's body copy, not metadata. | 🌐 |
| 2026-08-13 | **Change Password now asks for the current password first** (Peter — a real security gap: you could change the password without proving you knew the old one). Three fields when the user has a password: current, then new + confirm, with the current field separated by a wider 22px gap so one field reads as "verify" and the pair below as "set". **Creating** a first password is unchanged at two fields — there is nothing to prove. Added a "Forgot your password?" link for the change case, since someone who can't recall the current password is now blocked. | 🌐 |
| 2026-08-13 | Sponsor banner (Charity Mobile, Home only) **hides while the account menu is open** (Peter). The menu overlays the current tab but reads as its own page, so a Home-only ad sitting under it looked like a bug — and it never appears when the menu is opened from any other tab. | 🌐 |
| 2026-08-13 | **Progress track 3px → 4px** (Peter, modelled on Spotify): one change to the `WatchProgressBar` primitive lifts every blue track at once — Continue Listening rows, Listening History, the 155px Continue Watching cards, the hero slides, and the series-page card and episode rows. At 3px the bar was sized for when progress still sat over artwork; in the text block it read too light. **The reserved spacer on unstarted episode rows was raised to 4px in the same edit** — its height is coupled to the track, and a 1px mismatch silently reintroduces the baseline wobble it exists to prevent; both comments now say so explicitly. Verified after the change: all 8 rows of a sequential series at one row height and one title offset across watched / in-progress / unstarted. **Deliberately unchanged:** the two green audiobook bars (5px grid, 4px See All page) — separate feature — and the video player's white scrub bars. | 🌐 |
| 2026-08-13 | **Hero continue slides become the Continue Watching card at hero scale** (Peter): two lines instead of three — the **episode** you're mid-way through on line 1 ("The Meaning of Love"), track + "17min left" on line 2. The host line goes; artwork carries the series. The slide names the episode because **tapping now plays that episode** rather than opening the series page — reusing the existing `onPlayVideo`, no second play path. Browse slides are unchanged (series + "host · N episodes", tap opens the series) and were already two lines. The hero is full-width so "left" appears, per the established width rule. | 🌐 |
| 2026-08-13 | **Play circles removed from the Watch hero cards** — both the continue slides and the browse slide (Peter). That was the last place on the Watch tab printing anything on artwork; the whole card is the tap target, same call already made on the series hero. **The LIVE slide keeps its circle** — a different object with a different job (starting a live stream), deliberately left alone, not missed. | 🌐 |
| 2026-08-13 | Gate copy on **four of five** gates: headline "Save your place", sub "Login or create your free account." Leads with the benefit rather than an instruction. The series-page gate is excluded per Peter and keeps its single-line "Save your place with an account." | 🌐 |
| 2026-08-13 | ⚠️ **KNOWN GAP, deliberately left — the Continue Listening rows are inert.** `ContinueListeningRow` hard-codes `cursor: "pointer"` and its comment claims the whole row is the tap target, but neither call site (Home, Listen) passes an `onClick`; the Listening History rows are the same, pointer cursor and chevron with no handler. Not an oversight: **there is no audio player in the prototype at all** — no `<audio>`, no audio state, and the audiobook cards resolve to a static detail page whose "Preview" pill is also inert. `openVideoPlayer` is the only playback machinery, so wiring these means building an audio player. Peter's call to leave them for now. | 🌐 |
| 2026-08-13 | **Series page reworked — no blue strip at the series level.** DELIBERATE EXCEPTION to "blue strip at every Watch level", not an inconsistency: Watch home and All Series are navigational, a series page is content, and a strip there competes with the artwork it exists to show. Hero artwork now runs full-bleed to the top of the scroll area; back and share are dark translucent discs (`rgba(0,0,0,0.45)` + blur) rather than the blue strip's white-20% circles, because they sit over unpredictable artwork. **Their position is locked to the title bar's** — top 16 / inset 20, reproducing the bar's `16px 20px 14px` padding — so the controls do not shift when the bar fades in over them on scroll (verified: both states put back at x 112.5 and share at x 415.5, y 166.1). Change one and the other must change with it; it is commented in the code. **Hero is pure branding** — play button and bottom gradient scrim removed, and per Peter it is NOT tappable: playback always starts from a labelled origin (a Continue Watching card or an episode row). **Info block** reordered to title → "host · N episodes" meta line → description → Remind me pill (prayer series only); the title lives only here now so it wraps freely and never truncates, and the episode-count pill is gone (the count is in the meta line, and a pill should mean "tappable"). **Section headers** switched to the shared `WatchSectionHeader` (18px/800) — the page had been rolling its own 11px uppercase gray ones; the component gained a `pad` prop because the series page runs an 18px gutter against Watch home's 16px. **Episode list header is just "Episodes"** — "Newest first" / "In order" dropped, ordering logic untouched. | 🌐 |
| 2026-08-13 | **All padlock glyphs removed** (Peter): the gold lock shown beside signed-out section headers is off-brand — a padlock reads as paywall, and every gated feature here is free. Four instances gone (Watch home Continue Watching, Home Continue Listening, Listen Continue Listening, audiobooks grid) and the now-unused `lock` prop dropped from `WatchSectionHeader`. The gate card already explains itself in words. | 🌐 |
| 2026-08-13 | Join-sheet line: `FREE_LINE = "Always free. No subscription."` becomes `ACCOUNT_LINE = "Create your free account."` (constant renamed — the old name no longer described the string). The sheet's headline and bullets are all benefits, so nothing on it stated that continuing creates an account; this line does that job in one sentence, with "free" adjacent to "account" where it qualifies what's being created. Styling unchanged: 12.5px/600, #2d8a4e, centered, single element — short enough never to wrap. | 🌐 |
| 2026-08-13 | **Series-page back/share are now persistent, not per-surface** (Peter): one pair of discs lives outside the scroll area above the title bar, so they never move and are never swapped — only the fill crossfades, dark over artwork → white-20% once the blue bar is behind them. The bar carries just the title, with two 32px spacers holding the slots. Verified: back at x 112.5, share at x 415.5, y 166.1 in **both** states. | 🌐 |
| 2026-08-13 | Series-page Continue Watching gate states the ask in the headline — **"Save your place with an account."** — and drops the sub-line, rather than saying the same sentence twice. Leads with the benefit and names the account second; fits one line where the earlier "Create a free account and save your place." wrapped to two. NOTE: the other four gates still use headline "Start where you left off" + that line as the sub. For the record, the earlier "sync" changed only the shared **sub-line**; all five gates already carried the identical headline and sub, so no distinct titles were lost. | 🌐 |
| 2026-08-13 | **Scroll-triggered title bar on the series page** (Peter, Spotify pattern): a blue bar fades in once the hero clears the top, carrying back · series title · share. Title uses the same 24px/700 as every other blue bar and truncates to fit. Keeps back reachable deep in a 40-episode list, which the no-bar (Prime) alternative loses. | 🌐 |
| 2026-08-13 | **Account gate tightened**: the series-page gate was passed `pad="18px 0"`, making the frosted box ~36px taller than the card it hides; now `pad="0"` so the box is exactly the height of the real content. Sub-line shortened to "Create a free account and save your place." and **synced across all five gate instances** (series page, Watch home, Home Continue Listening, Listen Continue Listening, audiobooks grid) at Peter's call, so the gate reads identically everywhere. | 🌐 |
| 2026-08-13 | **Closed — floating discs over artwork.** The back disc lands on the "PROTECT YOURSELF — USE HOLY WATER" headline on Lenten Lessons on the Mass. Peter's call: not a real factor, the dummy artwork is temporary and official art is coming. No scrim, no inset change, no fallback. | 🔀 |
| 2026-08-13 | **Continue Listening now leads the Listen tab** (Peter) — the same three stacked rows and the same data as Home, above the Audiobooks card, because resuming is the reason most people open that tab. "See All" opens the shared Listening History page via a new `listenView: "listeningHistory"` branch; back returns to the Listen landing, not Home. Same AccountGate and lock for signed-out users. **Continue Watching card: title and progress row tightened into one unit** (Peter, proximity) — measured, the title sat optically 11.9px above its track against 9.4px below the artwork, so it grouped with the image rather than with its own progress. Resume-row margin 6→1 put it at 6.9px vs 9.4px; Peter then asked for ~2px back, so it sits at **3** — 8.9px vs 9.4px above. That left the two gaps within half a pixel of each other, so the air was then added **above** the title instead — card padding-top 8→9, giving 10.4px above vs 8.9px below. Adding above rather than below buys breathing room and strengthens the grouping at once, instead of trading one against the other; both card modes share the padding. Commented, including the fact that the row's own centring of a 3px track in an ~11px line box supplies most of the remaining gap. Continue Watching card titles were briefly raised to 14px to match the Continue Listening rows and **reverted the same day** — at 14px they read wrong beside the 11px New Episodes cards on the next row. Video card titles stay with their own family (155px cards 11px, 220px series cards 12px); the Continue Listening rows are a separate surface and keep 14px. | 🌐 |
| 2026-08-13 | **Bug (Peter): after "Don't Remind Me", the next Remind Me armed a reminder silently.** Cause: the flow gated on a `primedOnce` boolean — "have we shown the card before?" — instead of on whether the app may actually notify. Replaced with real permission state (`default` / `granted` / `denied`). The benefit-specific card now appears **every** time a reminder is switched on without permission; there is no "already asked" escape hatch, because a reminder that can't fire is a broken promise. "Don't Remind Me" cancels the reminder and leaves permission `default`, so the same card returns next time (exactly the re-prompt Peter asked for). OS "Don't Allow" sets `denied` — and since iOS won't show that dialog twice, the card switches to a **Settings variant** ("Notifications are turned off for Relevant Radio… Turn them on in Settings", button "Open Settings"), rather than offering a button that would do nothing. Open Settings grants permission as a demo shortcut, same convention as tapping the magic-link glyph. Once granted, reminders arm silently. | 🌐 |
| 2026-08-13 | Continue Listening polish (Peter): rows **re-inset to 16px**, flush with Articles and every other Home section — the 24px inset read as a misalignment rather than as separation (reverses the same-day decision; comment rewritten so the old rationale isn't left behind). Type **sized up a step for an older audience** — title 12.5→14, subtitle 10.5→12, time 10→11.5, on the Home rows and the Listening History page alike. Time-remaining gained right padding: it sat 7px off the card edge against a 9px artwork-to-text gap on the left and read as cramped; now 16px. Verified by measurement: rows and header share the Articles card's exact left/right edges. | 🌐 |
| 2026-08-13 | Progress moved off artwork into the text block and recoloured to accent blue so green means LIVE only; card metadata relaid out left-aligned as "date/episode · time left"; Continue Watching card titles truncate; episode rows reserve invisible track space so titles hold a constant baseline; Home Continue Listening rebuilt as three stacked rows mixing shows and audiobooks, with a new Listening History page behind See All. Audiobook Continue Listening surfaces deliberately untouched — separate feature. | 🌐 |
| 2026-08-13 | _Detail for the entry above._ **Nothing is printed on artwork** — `WatchProgressBar` rewritten from an absolutely-positioned overlay into an in-flow 3px track (radius 2, `rgba(22,32,44,0.13)`, fill `WATCH_PROGRESS` #3b6fa0) that flexes to fill its row; relocated at all four call sites (episode card, hero carousel continue slide, series-page Continue Watching card, episode list rows). Blue not green because **green already means LIVE / Now Playing** — "you paused here" and "on the air right now" cannot share a colour; after this change green appears only on live indicators and the Now Playing bar. Last play-circle overlay (series-page Continue Watching thumb) deleted. **Time format** abbreviated, no comma (`17min` · `1hr 26min` · `7hr 53min`); whether "left" appears is decided by WIDTH and commented as deliberate — full-width rows say "4hr 28min left", the 155px card says "17min" and lets the section header carry the meaning. **Reserved track space**: unstarted episode rows render a transparent spacer of the track's exact height so titles hold a constant baseline (verified: 8 rows, 81px each, title offset 19.4px across watched/in-progress/unstarted); signed out, neither track nor spacer renders. **Two reversals of this morning, both per the mockup and confirmed with Peter**: New Episodes cards regain the show name ("The Vatican Today · 27:14") and Watch-tab series cards regain their title, matching the All Series grid exactly. **Ranking** for the three Home slots: slots 1–2 most recent, slot 3 reserved for a long-form title — implemented as *most recently played* long-form rather than *longest-remaining*, because longest-remaining would surface Kristin Lavransdatter instead of Confessions, which Peter's content spec names. PRD product decision. | 🌐 |
| 2026-08-13 | **KNOWN INCONSISTENCY, DELIBERATE — do not "fix".** The audiobook Continue Listening surfaces (`ContinueListeningItem` 2×2 grid and the `allContinueListening` page, with its own See All) keep their green progress bars and current format. They are a separate feature and a separate conversation; audiobook resume will read green while everything else reads blue until that work happens. Peter's decision, not an oversight. | 🔀 |
| 2026-08-13 | **Watch search is real — series *and* episodes** (Peter): tapping the magnifying glass **replaces the browse content** (hero, prayer cards, all rows) with a search surface. Empty state: "Search all of Relevant Radio video" + "Start typing and matching series and episodes will appear here." Typing returns two grouped sections with counts — **Series** (matches title, host, or category → opens the series page) and **Episodes** (matches episode title or its series title → plays the video), both as thumbnail rows with the standard two-slot meta line; episodes capped at 20 with "Showing 20 of N". No-match state names the query. The episode index is built once with `useMemo` — `generateEpisodes()` randomises durations, so per-keystroke regeneration would make results flicker — and results are **deduped by series+title** because the dummy generator cycles a short title list for long series (real data wouldn't repeat; the demo shouldn't either). | 🌐 |
| 2026-08-13 | Watch hero card gains 10px above it — matching the 10px below the card to its pills, so it sits evenly between the blue strip and the pills (Peter). | 🌐 |
| 2026-08-13 | **Blue strip header extended to every level of the Watch hierarchy** (Peter): the **Watch tab** gets the same blue strip as All Series — "Watch" in white, "All Series" link beside it (now white; accent blue was invisible on blue), and the magnifying glass as a circular icon that **expands inline to fill the row** exactly like All Series. The **series page** gets the strip too: circular back · series title in white · circular share (both were floating buttons on the artwork). Hero video screen sits directly beneath the strip, then the description ("Every keynote and breakout from SEEK 2026."), pills, Continue Watching, episode list. The old black sticky title bar under the hero is gone — the title lives in the strip. Series page restructured to header + scrolling body so the strip stays put. | 🌐 |
| 2026-08-13 | **Hero carousel pills go grayscale** (Peter): focused pill dark gray `wC.text2`, the rest light gray `wC.text4` — same dark/light selection logic as the All Series category chips, so selection reads identically at every level. Retires the green active dot. | 🌐 |
| 2026-08-13 | **All card metadata goes black** (Peter — "I can't read that gray"): both slots of the shared `WatchMetaLine` and the episode card's right slot are now `#1a1a1a`. That kills the light gray on series cards (host + episode count), New Episodes (title + duration), and the series-page episode rows — **and retires the green "N min left" text** on every Continue surface: Continue Watching, the per-series continue block, Home Continue Listening, and the audiobook Continue Listening grid + full page. Progress **bars** stay green #2e7d32 (status stays visual; text is one colour). Partly reverses the 2026-08-12 "green = status" rule *for text only* — Peter wants to see how the flat treatment reads. | 🌐 |
| 2026-08-13 | Notification prompt: **OS "Don't Allow" also unsets the reminder** (Peter) — denying at the OS level means it can never fire, so leaving the toggle on promises something the app can't deliver. Same fix as "Don't Remind Me". | 🌐 |
| 2026-08-13 | **Notification permission prompt is benefit-specific + actually cancels (Peter, voice-memo review).** A signed-in user tapping "Remind me" now gets the prayer's own payoff, not a generic ask: "Never miss daily Mass" / "Never miss the Divine Mercy Chaplet" / "Never miss the Rosary" (new `reminderPhrase` field on `WATCH_PRAYERS`), body "We'll remind you at {time} each day so the reminder can reach you." Buttons re-tiered to **Allow Notifications** / **Don't Remind Me** (was "Later"). **Bug fixed:** declining left the reminder switched on — the prompt now remembers which prayer opened it (`notifPrayerId`) and unsets it, so "Don't Remind Me" means what it says. Verified: Chaplet toggle reverts "Reminder set" → "Remind me". | 🌐 |
| 2026-08-13 | **"New This Week" renamed "New Episodes"** (Peter). | 🌐 |
| 2026-08-13 | **Episode cards collapse to a single metadata line** (Peter): one row under the artwork — episode title on the left (flexes, truncates with an ellipsis against the right value), duration on the right for New Episodes ("42:08") and time-remaining for Continue Watching ("17 min left", green). **Show/artist name dropped entirely from both card types** — artwork carries series identity once real title-card assets replace the dummy art. Continue Watching keeps its progress bar and green accent; only the text layout changed. | 🌐 |
| 2026-08-13 | **Series row cards collapse to one line** (Peter): creator/artist left, episode count right ("FOCUS · 24 episodes"); series name removed — the artwork carries it. No date or timestamp at the series level. Applies to the horizontal rows (Featured Series, Fr. Rocky Teaching, Films…); **the All Series grid keeps its title** — that's the browse-and-search surface where the name has to stay readable (Peter's call). | 🌐 |
| 2026-08-13 | **All Series gets the standard blue subpage header** (Peter): blue strip with the title + circular back button, matching the title-detail pattern. Search stays as a magnifying-glass icon top-right and **expands inline to fill the header row**, autofocused and ready to type, with an X to collapse and clear (the always-visible search field below the title is gone). The blue strip is the only blue on the page — chips, grid, and body stay black-on-white. "All Series" links elsewhere (Watch header + the Featured Series see-all) now render in accent blue #3b6fa0, matching the "All Titles →" pattern. | 🌐 |
| 2026-08-13 | **Progress-bar track lightened** (Peter): the unwatched portion was a dark scrim that muddied the artwork — now a white translucent overlay (`rgba(255,255,255,0.55)`), so it reads visibly lighter than the image underneath. Applied to video episode cards and Home Continue Listening. Watched portion stays green #2e7d32. | 🌐 |
| 2026-08-13 | **Home Continue Listening card** (Peter): play-button overlay removed from the artwork — the whole card is the tap target; time-left text darkened to near-black (#2a2a2a, weight 600) to match the title's weight instead of reading as muted gray. | 🌐 |
| 2026-08-13 | Close-session triage: all ⬜ entries below stay pending by Peter's call — one deliberate Vision port after Father Rocky reviews the slice. Base re-pinned @ 7decf1f (cosmetic — the base's two commits were demo chrome applied to both files). | meta |
| 2026-08-12 | **Two-slot card metadata system (Peter's spec):** shared `WatchMetaLine` — left slot = identity (flexes, truncates via minWidth:0), right slot = quantity/status (pinned, never shrinks) — used on episode tiles, series tiles (3 lines→2), All Series grid, and series-page rows. Right-slot states: duration (gray) / "N min left" (green `WATCH_GREEN`, shared with progress bars) / "Watched" (gray). New This Week tiles gain the show name in the left slot. **Series ordering added to the data model**: `ordering: "sequential"` on Into the Breach + The Quest → detail pages sort ascending with "EPISODES · IN ORDER" and rows keyed by "Ep. N"; episodic series stay newest-first keyed by date (never both). Into the Breach got 8 real formation-series titles mapped ascending (Ep. 1 = "The Call"); sequential demo progress sits at Ep. 3 with Eps 1–2 watched. Verified incl. explicit truncation stress test (long host ellipses, "24 episodes" stays fully visible). | 🌐 |
| 2026-08-12 | **Gate copy rewritten benefit-first (Peter's spec):** headlines say the payoff, not the instruction — sheet: "Never miss the {prayer}" / "Never lose your place" / "Pick up right where you left off" / "Everything you love, saved"; green "Always free. No subscription." line under every sheet headline (competitive position vs. paid apps); bullets from one shared GATE_BENEFITS source, reordered so the triggering benefit leads; gate cards unified to "Start where you left off" + "Create a free account and we'll remember your place."; end-screen sub: "Always free. We'll remember where you stopped — and pick up right there next time." Menu-header headline kept (it's an action row). Verified: "Not now" visible without scrolling on the longest headline. | 🌐 |
| 2026-08-12 | **No badges over images**: NEW corner badges removed from New This Week (same rule that killed the timestamps). | 🌐 |
| 2026-08-12 | Continue Watching row is strictly in-progress content: the "UP NEXT" card removed (confusing in a resume row — next-up lives on the end screen and series pages). | 🌐 |
| 2026-08-12 | Episode card unified (Peter): exactly two video card types — episode (title + one gray time line) and series (title/host/episodes). New This Week drops the artist line and shows timestamps ("27:14"), matching Continue Watching; "min left" phrasing reserved for in-progress only. | 🌐 |
| 2026-08-12 | **Card metadata system (Peter):** one consistent recipe — black title (one line, ellipsis) · light-gray artist · light-gray time. Continue cards (video + Home audio) drop the artist and show "N min left"; New This Week shows artist + "27 min 14 sec" (no "4 new" chip); series cards reorder to title/host/episodes (uppercase kicker retired, incl. All Series grid). **Timestamp-over-image removed app-wide.** Home Continue Listening: 4 smaller cards (3 full + 1 peeking), 4px green bar matching video. Series-page rows show "Ep. N · date · N min left" when in progress. | 🌐 |
| 2026-08-12 | **Design decision (refined): white = control, green = status.** In-player bars (full-player scrub, mini-player line) are white — they're touchable controls on dark chrome. Progress-on-artwork (continue cards, episode thumbnails) + hero dots are green #2e7d32 — the app's continuity/status color. Blue #2f80ed retired. | 🌐 |
| 2026-08-12 | **Prime-style episode metadata**: "Ep. N · date · length" on episode rows ("Ep. N · length" on continue cards); Resume/Watched text lines and the ✓ badge removed — the progress bar does the talking (watched = full green bar). SEEK 2026 episodes got real titles (Msgr. Shea keynote, Sr. Miriam James, Fr. Mike Schmitz, …). | 🌐 |
| 2026-08-12 | **Sheet copy refined**: "…to save your place while watching / while listening" (playback is never gated — "continue watching" copy implied otherwise); Home gate headline now "Listening to your favorite shows?"; end-screen card headline enlarged (21→25px); series-hero play button frosted (translucent white + blur, dark glyph); more space between the episodes chip and Continue Watching. | 🌐 |
| 2026-08-12 | **Copy: "Create or log in to your free account" everywhere** (menu card, sheet default + all contexts). Gates become question-led: "Watching video?" (Watch + series) · "Listening to Relevant Radio?" (Home) · "Listening to Audiobooks?" (Listen), each with sub "Create or log in to your free account to save your place." | 🌐 |
| 2026-08-12 | **Gold retired from progress UI**: progress bars now bright blue #2f80ed (and taller, 4px); hero carousel dots blue; "65% — resume"/"Watched" text replaced by clean third-line **Resume** (blue) / **Watched** (green); episode rows show title + duration only (dates + Ep. N removed); per-series continue card matches (title / duration / Resume). | 🌐 |
| 2026-08-12 | **Series hero is now tap-to-play**: centered play button starts the next-up episode (signed in: in-progress → first unwatched; signed out: top of list). Hero carousel CONTINUE pill removed; continue-slide subtitles show host only (no "Ep. 4 of 12"). | 🌐 |
| 2026-08-12 | **Removals/fixes**: Subscribe button gone from all series pages (only prayer series keep Remind me); red LIVE DAILY chip removed from prayer series pages; account page shows name only (no email) with Sign Out + Delete as flush rows; Change/Create Password = exactly two fields (new + confirm); reminders menu row count now "N ON" (caps); end screen v5 = one blue card (centered icon → "Create your free account" → dictated copy → button) + white "Share this video" button. | 🌐 |
| 2026-08-12 | End-screen v4 (Peter): headline sized to one line (34→27px, nowrap); blue-card copy consolidated to "Create or log in to your free account to save your progress." (subtitle removed); Share card → thin blue rounded-rect button "Share this video" (the Next-Up-format share card read as confusing). | 🌐 |
| 2026-08-12 | End-screen v3 (Peter): headline "Create your free account", subtitle "Just finished a video? Don't lose your place…"; blue card taller (24px pad), title 19px, 22px gap before the button; "Sign up or log in" 16px, person icon removed. | 🌐 |
| 2026-08-12 | **End-screen copy/layout pass (Peter):** subtitle "Create your free account and save your progress as you watch the videos."; blue card gets a bold title "Create or log in to your free account" + subtitle "Pick up right where you left off on every device."; Share is now a **card matching the Next Up skeleton** (thumb · SHARE kicker · episode title · series · round share button), placed **above** Next Up with extra spacing. | 🌐 |
| 2026-08-12 | **Signed-out post-video end screen** (Peter): account prompt takes the hero — "Don't lose your place" headline + gradient "Sign up or log in" card (opens the sheet with the save-your-place context; end screen resolves to the signed-in layout after joining). Next Up card kept below; Share demoted to a small pill. Signed-in end screen unchanged. | 🌐 |
| 2026-08-12 | **Player icon row trimmed to share + cast** — sleep-timer and queue/series buttons removed per the PRD cut (prayer-request button was already absent). | 🌐 |
| 2026-08-12 | **Email flow corrected to the industry-standard neutral pattern** (Peter's Hallow field test): no client-side account detection — one "Check your email" screen for everyone; **"What's your name?" moved post-auth** (new accounts only, after the magic link "authenticates"); "create one" removed from the sheet ("Or log in with your existing password here" only); **password creation moved to Account** — state-aware Create a Password / Change Password row (create = single field + helper copy; sets the flag and flips to Change). Password login marks the account as having a password. PRD (repo + ClickUp) updated incl. the never-a-raw-404 magic-link landing-page requirement. | 🌐 |
| 2026-08-12 | **Build PRD written** — `prd-vod-user-accounts.md` rewritten in the house PRD template as the engineering handoff for the bundle (VOD + accounts + account-based reminders; player ships share+cast only — sleep timer, queue button, prayer-request submission cut). Published to ClickUp (PRDs → Drafts). Slice = normative visual spec. | meta (spec, not a feature change) |
| 2026-08-12 | **"Log in to your free account" reframe** (Peter): all gate + sheet headlines now log-in-led ("…to save your place" on gates; "…to continue watching / continue listening / save your place in audiobooks / enable prayer reminders" on the sheet); menu card headline "Log in to your free account" with the person glyph in a circle. | 🌐 |
| 2026-08-12 | **Smart email flow**: recognized emails (demo: mary.thompson@gmail.com) skip the name screen and get "Prefer to use a password? Log in or create one"; new emails get name capture and "Create one" only. **Forgot your password?** link on the password screen → Reset-your-password screen (email + Continue → neutral confirmation); Help's Forgot Password routes there too. | 🌐 |
| 2026-08-12 | **Sheet buttons re-tiered to match Hallow**: Continue with Email black (primary), Apple + Google white. | 🌐 |
| 2026-08-12 | **Signed-in identity card v2**: gradient (matches the signed-out card), avatar + name only — no email, no inline sign-out; tapping opens Account. **Account page**: Edit Profile / Update Email / Change Password, then separated **Sign Out** above **Delete My Account**. | 🌐 |
| 2026-08-12 | Series page: divider under the buttons row removed. | 🌐 |
| 2026-08-11 | **Name capture + honest magic-link copy**: email path gains a "What's your name?" screen (one full-name field, parsed to first/last; Apple/Google supply the name); check-email copy now serves new + returning users ("We sent a link… if you're new, the same link creates your account"); "Prefer a password? Log in or **create one**" → new Create-a-password screen. Typed name/email become the signed-in identity (header initials update live). | 🌐 |
| 2026-08-11 | **Account area**: identity row opens **Account** (identity summary + Edit Profile · Update Email · Change Password · Delete My Account). Edit Profile: avatar color gallery (6 RR-palette presets) + Your Photos/Camera (visual), first/last name fields, Save — header + menu update live. Delete: iOS-style confirm → resets to signed-out. | 🌐 |
| 2026-08-11 | **Per-surface gate/sheet headlines** (Peter): "…to continue watching" (Watch) · "…to continue listening" (Home) · "…to save your place in audiobooks" (Listen) · "…to track progress" (series) · "…to enable **prayer** reminders" (reminders). Gate card + sheet always use identical wording. | 🌐 |
| 2026-08-11 | **Series-page polish**: LIVE DAILY chip moved off the hero to below the description; Remind me pill white (was grey — bled into the page bg); series gate breathing room (subtitle dropped, taller padding). | 🌐 |
| 2026-08-11 | **Account sheet rebuilt as a Hallow-style 4-screen flow** (Peter's reference; RR-styled, not cloned): options (contextual headline + ✓ benefits + **Continue with Email** primary / Apple / Google) → "What's your email?" → "Check your email!" **magic link** (+ "log in with your existing password here") → password screen w/ eye toggle. **Help** on email/password → iOS action sheet: Forgot Password (neutral success alert) · Contact Us (mailto info@relevantradio.com). Tapping the magic-link glyph completes sign-in (demo). | 🌐 |
| 2026-08-11 | **Contextual sheet headlines** — "Create your free account to enable reminders / to save progress / to track progress" depending on entry point; default "Create your free account". | 🌐 |
| 2026-08-11 | **One CTA everywhere: "Sign up or log in"** — gate pills, menu account card (replaces Sign up + Log in pair). Gate copy rework: headline "Create your free account to …" + feature-benefit subtitle ("Pick up right where you left off." / "Your audio, right where you paused it." / "Keep your place in every episode."); "It's optional…" line retired. | 🌐 |
| 2026-08-11 | **Home tab: Continue Listening row** (under Articles — the real app has an under-designed version): 3 audio cards (cover, play overlay, green progress bar, time left), account-gated with the same bounded blur module. | 🌐 |
| 2026-08-11 | **Series Detail cleanup** (Peter): single back button (sticky-bar back removed), title bar hairline removed, title enlarged to a real title (22px), host line removed (description only). **Live-prayer series (Mass / Divine Mercy / Family Rosary): Subscribe replaced by the daily-prayer "Remind me" pill** — same look as the Watch rows, same shared state (gates + priming flow included); Subscribe remains for non-prayer series. | 🌐 |
| 2026-08-07 | Menu spacing: card-to-rows gap halved (card marginBottom 18→10, row list marginTop 8→0); beta card keeps its own 18px offset when visible. | 🌐 |
| 2026-08-07 | **Account card v4** (Peter): person icon + "Create your free account" on one line; more negative space (headline→checks 16px, checks→Sign up 20px). **Stock glyphs** replace the custom drawings: Prayer Requests = Lucide message-circle-heart (blue), Give Now = Lucide hand-heart (red). | 🌐 |
| 2026-08-07 | **Account card v3 + menu flattened** (Peter): card headline is now "Create your free account" (21px, kicker removed), person icon enlarged (26px, top-left), checkmark text up to 14px (fills card width). Red **Give Now banner removed** — menu rows now open with **Prayer Requests** (blue praying-hands icon) · **Give Now** (red heart-in-hand icon) · **Daily Prayer Reminders** (gray bell) — colorized icons carry the hierarchy, gray for everything else. | 🌐 |
| 2026-08-07 | **Gates become bounded locked modules** (Peter + UX review): rounded-rect card container (16px, hairline border, soft shadow) around the blurred content, plus a **white scrim (82%)** so headline/reassurance text always clears contrast. Reassurance text darkened. Applies to all three gates (Watch row, Listen grid, Series Detail). | 🌐 |
| 2026-08-07 | **Account card + sheet aligned**: three ✓ benefits now on the menu card (copy simplified per Peter: save your progress while watching · daily prayer reminders · pick up where you left off listening); sheet retitled **"Create your free account"** with the same three checks (echoes the card kicker). | 🌐 |
| 2026-08-07 | **Menu hierarchy**: Prayer Requests demoted from blue banner to a plain menu row (heart icon, next to Daily Prayer Reminders) — one blue object (account card), one red (Give Now), one orange (beta). | 🌐 |
| 2026-08-07 | **BETA demo pill visible on every screen** (was Home-only — made its state invisible elsewhere and looked buggy): sidebar restructured so tab-specific time pills come and go while BETA always renders at the bottom of the stack. Applied to the Vision same-commit (chrome). | 🌐 ported (chrome, same-commit) |
| 2026-08-05 | **Accounts UI copy/type pass (Peter's review):** all serif headlines → DM Sans (gates, sheet, priming card, in-menu reminders header). Account sheet rebuilt benefit-first: headline "Sign in or create an account to track and save your progress", three ✓ benefits, primary **Sign up** + "Already have an account? **Log in**", footnote removed. Gates: pill → "Sign up", reassurance → "It's optional, but it enhances your experience." Menu card: kicker "CREATE YOUR FREE ACCOUNT", benefit headline, Sign up pill + Log in link, "Everything stays free." removed. PRD §5/§6 synced. | 🌐 |

_New changes get appended below as ⬜ pending until triaged._

---

| 2026-08-14 | **FUNNELLED TO THE VISION — everything.** Peter's call at close session: all 85 pending entries above ported into `index.html` in one pass. Verified first that the Vision had NOT diverged — zero commits to `index.html` since the slice's base pin `7decf1f`, zero components and zero user-facing strings present in the Vision but absent from the slice (the only 7 that differed were values this slice deliberately replaced: the retired uppercase kickers, "New This Week", and the removed Subscribe button). The slice was therefore a strict superset, so the port carried the slice's content wholesale rather than re-typing 85 hand-edits — same result, no transcription risk. The Vision keeps its own identity: PROTO front matter, its title, no `<base href>` (root, not a subfolder), and the legacy `branchTitles` script stays removed. | meta (the port itself) |
### The Feedback Form (built 2026-08-13/14 as `slices/user-feedback.html`, merged in 2026-08-14)

> **Retired 2026-08-14 (Peter):** this slice was absorbed into `slices/video-on-demand.html`, which was
> renamed **Video On Demand + User Accounts + Feedback Form**. Two slices never made sense — the feedback
> form's signed-in path, the account page and the join sheet are all the same account model VOD owns, and
> the menu reorder touched rows VOD already had. The whole feature (all 21 items below) was applied to the
> VOD slice and had already been funnelled into the Vision. The file moved to `slices/archive/`; git keeps
> the full history. **Read the entries below as history of the VOD slice's feedback work.**

Copied from `slices/video-on-demand.html` @ `a188acb` on 2026-08-13. Built on VOD because
this feature is **account-gated** and VOD is the only slice with user accounts — hence
`dependsOn: slices/video-on-demand.html` (accounts must ship before feedback can).

**The premise (Peter, 2026-08-13):** we have no way for users to give us feedback in the
app, and the separate beta form shouldn't be a second thing to maintain. **One portal**,
inside the app, that knows which build you're on and dresses itself accordingly.

| Date | Change | Status |
|---|---|---|
| 2026-08-13 | Slice created as a copy of `slices/video-on-demand.html` @ a188acb. | meta (creation, not a feature port) |
| 2026-08-13 | **Orange "App Beta Feedback" hero card removed** from the More menu. It was a second entry point and a second visual tier; the portal is one row now. | 🌐 |
| 2026-08-13 | **One build-aware menu row** — "Feedback" (muted icon) in the normal build, "Beta App Feedback" (orange icon) in the beta build. Styled identically to every other menu row; sits directly under **Give Now**, above Daily Prayer Reminders. | 🌐 |
| 2026-08-13 | **One form, two dressings** — same fields either way; the beta build gets the orange banner + "What's working? What's not?" + the device/version footnote, the normal build gets house blue + "Tell us more". | 🌐 |
| 2026-08-13 | **Manual email field removed.** Signed-in users see a "SENDING AS · name · email" row instead — the account is the reply address. | 🌐 |
| 2026-08-13 | **Account required to submit** — signed-out users get the standard `AccountGate` over the real form ("Create a free account to send feedback."), opening the existing join sheet with a feedback-first benefit bullet. | 🌐 |
| 2026-08-13 | **Help escape hatch, always visible** (`appfeedback@relevantradio.com`) — "Having trouble creating an account?" when signed out, "Need help with something?" when signed in. Account trouble must never be the reason we don't hear from someone. | 🌐 |
| 2026-08-13 | **"What kind of feedback?" chips** (Something's broken / I have an idea / I love it) — gives the vestigial `feedbackType` state a job and lets submissions be routed without a human triaging every line. Proposed, not requested — easy to cut. | 🌐 |
| 2026-08-13 | Send button disabled until there's text (the old form accepted empty submissions). | 🌐 |
| 2026-08-14 | **Direction settled (Peter, after four rounds of reasoning — see the audio scripts at the repo root).** The Help-form idea is dead: **Contact stays exactly as it is** (the existing embedded web form, owned by Donor Relations, already working). Rationale: that form is the *website's* form embedded in the app, so replacing it in-app would fork it and create two general contact forms to keep in sync — the exact duplication this slice exists to eliminate. Also discovered the dropdown already has a **Mobile App Support** option, so app feedback has been arriving all along; the gap was never collection, it was **routing to Peter**. | 🌐 |
| 2026-08-14 | **Label → "App Feedback"** (normal) / **"Beta App Feedback"** (beta). Was "Feedback"/"Beta App Feedback". | 🌐 |
| 2026-08-14 | **Contact moved up** out of the `menuItems` list to sit directly under App Feedback. Two adjacent doors — one scoped to the app, one for everything else — so the labels do the sorting with no explanatory copy. Destination unchanged. | 🌐 |
| 2026-08-14 | **Daily Prayer Reminders moved off the main menu into the account page**, directly above Edit Profile. Reminders are per-account settings, so they belong with the things you tune about your account; it also keeps the main menu to destinations only. Its back arrow (and the global header back) now step to the **account hub**, not the main menu. | 🌐 |
| 2026-08-14 | **Account gate removed — no account required to submit.** Today's Contact form requires no account, so gating feedback would have shipped a regression. The `AccountGate` + join-sheet wall over the form is gone; the form is simply open. | 🌐 |
| 2026-08-14 | **Email required instead.** Signed in → the "SENDING AS" identity row (nothing to type). Signed out → a required email field with microcopy underneath: "**Create a free account** and we'll fill this in for you next time." The account is now offered as a *reward* (skip this next time) rather than charged as a *toll* — captures the submission first, pitches second. | 🌐 |
| 2026-08-14 | **Toggle is required with nothing pre-selected** (Peter's original instinct was to default it to "Something's broken"; changed after review). A default would read as an answer, and defaults win — most submissions would arrive tagged "broken" regardless of content, corrupting the one field that measures friction and flooding Donor Relations. The safety concern it addressed is already covered: everything lands in the ClickUp list regardless. Precedent: the real Contact form's two required dropdowns already ship as "PLEASE SELECT". Chip order set to I love it · I have an idea · Something's broken. | 🌐 |
| 2026-08-14 | Send disabled until **toggle + text + (signed in or email)**. | 🌐 |
| 2026-08-14 | **Confirmation forks on the promise, not the words.** "Something's broken" routes to Donor Relations, so it promises a reply — "Someone from our team will get back to you, usually within one business day" + the reply address. The other two keep the existing thank-you, which deliberately promises nothing. | 🌐 |
| 2026-08-14 | **Help-email escape hatch replaced with a Contact pointer** — "Something that isn't the app? Donations, prayer requests, shows, or your station — use Contact." A pointer, not a disclaimer: it names where to go rather than what this form refuses to do. (The old copy read as declining to help, which Peter flagged.) | 🌐 |

| 2026-08-14 | **Contact page cloned into the prototype** — a faithful mock of the page that ships today, reached from the Contact row and from the App Feedback card. Deliberately NOT redesigned and NOT native-styled: web typography (Arial), gray box inputs, red asterisks, blue underlined links, the reCAPTCHA, the real "Show or Department" option list (Accounting/Address Updates · General Listener Comments · Affiliate Information · Website Support · **Mobile App Support** · Parish Ambassador Team · divider · the ten shows), all three green phone buttons with their real numbers, and the HQ mailing address. It exists so the App Feedback design can be judged side by side against what actually exists. **Owned by Donor Relations — do not restyle.** Two placeholders: the red hero is a gradient standing in for the tinted laptop photo (asset needed), and "My Station" reproduces only PLEASE SELECT (the real station list lives on the site). | 🌐 |
| 2026-08-14 | **"Sending as" split onto three lines** (Peter) — label, then name, then email, instead of name · email crowded onto one line. | 🌐 |
| 2026-08-14 | **Non-app card headline rewritten** (Peter — the old one wasn't clear): "Something that isn't the app?" → **"Need to reach us about something else?"**, sub "For donations, prayer requests, shows, or your station, use **Contact**." The card now navigates straight to the Contact clone rather than dumping you back on the menu. | 🌐 |

**Back-end spec for dev (not prototype):** one ClickUp list holds everything. Auto-captured fields: source (`app-feedback`), toggle value, build (beta/production), app version, email. "Something's broken" **dual-writes** — an email to Donor Relations in the shape their existing queue expects (so their process doesn't change at all) *plus* the ClickUp record. Two saved views: response-owed (Donor Relations) and everything (Peter, CSV-exportable). Obligation must be structural — auto-assignee + auto due date — not cultural. Replies go out from the ticket, not a personal inbox, or the record keeps the question and loses the answer.

**Two asks that aren't builds:** (1) get Donor Relations to copy **Mobile App Support** + **General Listener Comments** submissions into the same ClickUp list — no process change for them, and it gives Peter the one-list architecture without building anything; (2) ask the website team to make **My Station** optional on the .org contact form — it's a required broadcast-era field that blocks app-only listeners, and because the app embeds the website form, fixing it there fixes both surfaces. Also worth pulling the current Mobile App Support volume as a pre-launch baseline.


**Funnelled 2026-08-14 (session 25 follow-up):** the entire feature ported into the Vision (`index.html`) once the VOD/User Accounts slice landed there — accounts were the blocker, since the form's signed-in path, the account page and the join sheet all depend on them. Ported as one unit: menu reorder (App Feedback + Contact adjacent, Daily Prayer Reminders moved to the account page), the FEEDBACK_* build constants, the gate's feedback bullet, the Contact page clone, and the form + forked confirmation. The Contact clone is a **current-state mock, not vision-state** — it reproduces the six-required-field web form deliberately so App Feedback can be judged beside it; say the word if you'd rather the Vision not carry it.

_New changes get appended below as ⬜ pending until triaged._

---

---

| 2026-08-14 | Now Playing bar drops flush to the nav when the account menu is open on Home (Peter). The Home offset (128px, vs 72 elsewhere) exists to clear the sponsor banner; once the banner started hiding under the menu the player kept its offset and floated above an empty 56px gap — a gap Listen and Watch never had. Both the Now Playing bar and the video mini-player now drop with the banner. Verified both directions: menu open → 72px, gap to nav 0; menu closed → 128px with the banner back. | 🌐 |
| 2026-08-14 | **PRDs split and brought current (Peter's direction).** The bundled PRD became two: `Roadmap/prd-vod-user-accounts.md` (v3 — now current with the full polish pass: resume/progress system, series-page rework, Continue Listening + Listening History, loading/error states, gate copy, reminders inside Account; plus a new **§Engineer Walkthroughs, WF-1…WF-14** — the exact click-paths engineers must run, centered on the Slack-pattern email flow: magic link verifies the email, THEN name capture, THEN password becomes creatable) and `Roadmap/prd-feedback-contact.md` (new — App Feedback / Beta Feedback / Contact, the Donor Engagement–Donor Relations support model, and the routing philosophy). **New routing decision encoded:** beta "Something's broken" goes to the app team, never Donor Relations — they aren't in the beta; UI identical, destination forks on the build flag; flagged for confirmation at Peter's Donor Relations sync, which the PRD marks as a launch gate. Register row renamed: Feedback Form → Feedback & Contact (One Portal). | meta (spec, not a feature port) |
| 2026-08-14 | **Feedback form pass (Peter, voice review — applied to slice AND Vision in the same commit).** (1) Toggle chips get **stroke icons** (heart / lightbulb / wrench — never emoji) above the label, with the whole chip column-centered: "Something's broken" wraps to two lines, so the one-line labels used to float to the top of their taller boxes. (2) **Beta intro** gains headline "Help shape the app" + new copy ("You're testing an early version… will have bugs or errors… directly shapes the app's development."). (3) **Production intro** gains a deliberately non-committal headline — "Tell us what you think" — because production must not promise influence. (4) Footer card headline shortened to one line: "Need us for something else?". (5) **Beta confirmation never promises a reply** — "Something's broken" in beta shows the plain thank-you, matching the routing decision that beta reports go to the app team, not Donor Relations; the reply promise now exists only where Donor Relations actually receives (production + broken). | 🌐 |
| 2026-08-14 | Beta intro copy shortened (Peter): "As you test new features, tell us what's working and what's not. Your feedback is reviewed daily by our team and directly shapes the app's development." Drops the bugs-and-errors warning sentence; adds the "reviewed daily" claim. Headline "Help shape the app" unchanged. Slice + Vision + PRD in the same commit. | 🌐 |
## `slices/archive/prayer-reminders.html` (prayer slice — ARCHIVED 2026-08-14; formerly `prd/on-device-prayer-reminders-watch-tab`)

| Date | Change | Status |
|---|---|---|
| 2026-06-04 | Entire Session 16 Watch-tab synthesis + live/countdown/reminders/toggles/colors/data | 🌐 ported to the Vision |
| 2026-06-04 | Give Now → red button | 🌐 |
| 2026-06-04 | Limited video player (remind/play/pause/share/cast only) | 🔀 slice-only |
| 2026-08-03 | Base re-pinned @ e1dc175 — cosmetic reintegration (meta-only base changes). | meta |
| 2026-08-03 | Base re-pinned @ 7014064 — converging re-pin (base's front matter moved in the prior re-pin commit). | meta |
| 2026-08-14 | **Archived** — moved to `slices/archive/` from Proto's board. Delayed until after user accounts so reminders ship with them rather than being built twice; reminders now ship account-based inside the VOD bundle. | meta |

---

## `slices/live-video.html` (live-video slice — foundational; formerly `prd/live-video-in-app-home-screen`)

The foundational PRD (Live Video on Home Screen). Its changes fed **both** the
prayer-reminders slice and the Vision (hand-port — Claude owns it).

| Date | Change | Status |
|---|---|---|
| 2026-06-05 | Synced meta-infrastructure from `main` (rich CLAUDE.md + Roadmap/) onto this branch; rebuilt complete 1–16 session-log (union of all branches); reactivated this slice as foundational | meta (not a feature port) |
| 2026-06-05 | Skill update: "start session" now presents a table of active branches for Peter to select | meta |
| 2026-06-05 | Home live card: added a 4th demo toggle **"Event"** — a generic *Relevant Radio Live* event ("Live Now" heading, `special-event-livestream.mp4` clip, RR-blue hero bar, brands the video player + end screen on tap-through; end-screen RR logo centered/padded so it isn't clipped). **Hand-port** to `main` + prayer slice (branches diverged). | 🌐 ported (hand-port → `main` `415cb2c`, prayer slice; adapted to each branch's image-based / countdown architecture; Babel-parse verified, visual review pending) |
| 2026-06-05 | Video end screens: removed the top-right "Done" pill; both end screens now use the same centered top **down-arrow** (`collapseToMini`) as the active player — unified close/minimize gesture across the whole player flow. | 🌐 ported (`main` + prayer slice) |
| 2026-06-05 | **Mini player plays the live video clip** (autoPlay/loop/muted) instead of a frozen thumbnail when the series has one; falls back to static series image for on-demand. | 🌐 ported (`main` + prayer slice; added a `getLiveClip` helper on each, since their players were image-based) |
| 2026-08-03 | Base re-pinned @ 5d6b62c — cosmetic reintegration (meta-only base changes). | meta |
| 2026-08-03 | Base re-pinned @ ea6a41a — statuses-removal meta edit; cosmetic. | meta |

_Feature changes get appended below as ⬜ pending until triaged for funnel to prayer-reminders + the Vision._

---

## `slices/archive/beta-feedback.html` (beta-feedback slice — ARCHIVED 2026-08-14; formerly `prd/beta-feedback`)

In-app Beta Feedback (PRD "Beta Testers Feedback Form"), built as a visual prototype — UI/UX
only; the ClickUp / Lambda / Firebase backend is the dev team's. Discrete app-wide menu chrome.

| Date | Change | Status |
|---|---|---|
| 2026-06-05 → 06 | **Beta Feedback flow** — orange mood-board **card** (grain + bloom, "Help shape the app", white "Submit Feedback" pill) at the top of the More menu; branded **form** (orange PageHeader-style banner, titled fields w/ in-field placeholders, orange Send Feedback); orange **confirmation** (orange checkmark + kicker, "Thank you" + bolded mission, orange Done). `BETA_BUILD`-gated (kept visible on `main`). Iterated heavily on the menu entry (in-line button → solid orange → white-outline → titled section → final card) and copy. | 🌐 ported (`main` `5dc0706`, ff via integration branch) |
| 2026-06-05 | **Give Now** button red softened `#d32f2f` → `#cc4b4b` (only that button; global `colors.red` unchanged). | 🌐 ported (`main` `5dc0706`) |
| 2026-08-03 | Base re-pinned @ 5d6b62c — cosmetic reintegration (meta-only base changes). | meta |
| 2026-08-03 | Base re-pinned @ ea6a41a — statuses-removal meta edit; cosmetic. | meta |
| 2026-08-14 | **Archived** — moved to `slices/archive/`. Ported to the Vision, then superseded: the VOD slice's App Feedback form replaced this flow. The MANIFEST entry is kept (`archived: true`) so lineage and this record survive. | meta |

---

## `slices/archive/user-feedback.html` (User Feedback (One Portal) — ARCHIVED 2026-08-14)

Prototyped as its own slice 2026-08-13/14, then merged into
`slices/video-on-demand.html`: its signed-in path, account page and join sheet are the same
account model VOD already owns, so two slices made no sense. **All 21 feature entries live in
the VOD section above** — they were funnelled from here and are not duplicated.

| Date | Change | Status |
|---|---|---|
| 2026-08-13 | Slice created off `slices/video-on-demand.html` @ a188acb — the only slice with user accounts, and this feature is account-gated. | meta (creation) |
| 2026-08-14 | Feature applied to the VOD slice; this file retired to `slices/archive/`. | meta |
| 2026-08-14 | **Archived** — merged into another slice (not shipped-and-done). Same one archive state either way; the note carries the reason. MANIFEST entry restored and flagged `archived: true` — deleting it had hidden the slice from Proto entirely. | meta |
